name: image_crop_plus_example
description: Demonstrates how to use the image_crop_plus plugin.

version: 1.0.0+1

environment:
  sdk: ">=2.0.0-dev.68.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  image_picker: ^0.7.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  image_crop_plus:
    path: ../

flutter:
  uses-material-design: true
